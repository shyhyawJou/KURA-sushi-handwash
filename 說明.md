# 執行程式
```
pkill -f simulate_ui_kura.py; rm -rf log && nohup python app_ai.py &
pkill -f app_ai.py
```

# 清理 terminal
```
clear && printf '\e[3J'
```

# 看 stream 畫面
```
ssh -L 9527:localhost:9527 root@bi.ethwu.com -p 12352

ssh -L 9527:localhost:9527 -p 12363 root@bi.ethwu.com
ssh -L 9527:localhost:9527 user@10.1.13.230
ssh -L 9527:localhost:9527 root@192.168.1.22
ssh -L 9527:localhost:9527 root@192.168.1.109

ssh -L 8550:127.0.0.1:8550 root@bi.ethwu.com -p 12361
ssh -L 8550:127.0.0.1:8550 root@192.168.1.109
```

# ping
```
ping 8.8.8.8
ping 57.181.173.247 -p 5201
ping bi.ethwu.com
```

# 更改上傳檔案的程式
```
crontab -e
```

# 看 debug level 訊息
```
journalctl -u kurashushi_handwash -f
```

# tflite -> dla
```
ncc-tflite ramen_float32.tflite -arch mdla3.0 -d ramen_float16.dla --relax-fp32
```

# 影片切幀
```
python video_to_img.py -wh 640 480 -o frame_video_only_predict/ -n 10 -s only_ai_predict -i video_only_predict/
```

# 影片切段
```
ffmpeg -i video/20260410_1.mp4 \
-ss 225.83 -to 456.67 \
-vf "fps=20" \
-c:v libx264 -crf 10 -c:a aac -b:a 192k \
20260410_gg.mp4
```

# 產生 gif
1. 產生調色盤
ffmpeg -i video/20260804_l.mp4 -vf "select='between(n,2475,2676)',setpts=PTS-STARTPTS,palettegen" -vsync vfr palette.png

2. 用調色盤輸出 gif
ffmpeg -i video/20260410_1.mp4 -i palette.png -filter_complex "select='between(n,2475,2676)',setpts=PTS-STARTPTS[x];[x][1:v]paletteuse" -vsync vfr 20260410_test.gif

# 影片修復工具
- 編譯
```
git clone https://github.com/anthwlock/untrunc.git
cd untrunc
make
```
- 執行
```
untrunc -s 20260805_3_p4.mp4 20260805_4.mp4
```

# git
```
git diff main..read_video > z.patch

git apply --check z.patch
```

# tflite -> dla
```
ncc-tflite tech_demo_0207_float32.tflite -arch mdla3.0 -d tech_demo_0207_float16.dla --relax-fp32
```

# mqtt pub
- 登入
```
mosquitto_pub -h localhost -t "handwash/system" -m '{"cmd": "Login", "user": "ごじゅうおん", "id": "00001", "side": "right"}'

mosquitto_pub -h localhost -t "handwash/system" -m '{"cmd": "AILogin", "side": "right"}'
```

- 沒手登出
```
mosquitto_pub -h localhost -t "handwash/system" -m '{"cmd": "Reset", "side": "right", "time": "0.0"}'

mosquitto_pub -h localhost -t "handwash/system" -m '{"cmd": "Reset", "side": "right", "time": "0.0"}' && mosquitto_pub -h localhost -t "handwash/system" -m '{"cmd": "AILogin", "side": "right"}'
```

- 切換步驟
```
mosquitto_pub -h localhost -t "handwash/process" -m '{"step_id": "Step1", "washcount": "0", "washtime": "0.1398789882659912", "side": "right", "trigger": true}'
```


# rclone
- tree
```
rclone tree onedrive:upload
```

# 把當天的影片壓縮成 .zip
```
find clips/ -name "20260823" -type d -print0 | xargs -0 zip -r 20260823_clips.zip
```

# 設定未來某一時間上傳更新程式
```
at 07:15 <<'EOF'
cd /media/user/disk2/mateo/kura_handwash
. /media/user/disk2/mateo/py310_venv/mmlab/bin/activate
python3 批量更新/test_connect_machine.py >> /media/user/disk2/mateo/kura_handwash/zat.txt 2>&1
EOF
```

# 查 NTC 伺服器校正紀錄
```
journalctl -u systemd-timesyncd
```